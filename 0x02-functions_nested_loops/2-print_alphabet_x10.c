#include <stdio.h>
#include "main.h"
int main ()
{
   char ch;
   int a;
   for(a=0; a<10; a++)
   {
       {
      putchar('\n');
        }
   for(ch = 'a' ; ch <= 'z' ; ch++)
   {
      putchar(ch);
   }

   }
   return(0);
}

